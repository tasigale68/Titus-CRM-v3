# Titus CRM Website — Handover Document
**Date:** 2026-03-11
**Session:** Administrator Portal upgrade — 3-Pipeline CRM + Email Automations

---

## What Was Built This Session

### 1. Email Send & Receive (info@titus-crm.com)
- **Cloudflare Email Routing** configured — `info@titus-crm.com` → Worker `tituscrm`
- **Inbound flow:** Email arrives → Cloudflare routes to Worker → PostalMime parses → stores in `admin_emails` table + forwards to `tasigale68@gmail.com`
- **Outbound flow:** Admin portal Compose → Edge Function `/admin/email/send` → Resend API → stores in `admin_emails`
- **RESEND_API_KEY** set in Supabase secrets (project `octdvaicofjmaetgfect`)
- **Domain verified** in Resend for `titus-crm.com`

### 2. Tags System
- Added `tags` column (text[]) to `waitlist` table
- New waitlist signups auto-tagged with `WAITLIST FORM`
- Tags are editable in Lead Detail Panel (add/remove, purple badges)
- Tags shown on Kanban cards and Export table
- Tags included in CSV/XLS exports

### 3. 3-Pipeline CRM (19 Stages)
Replaced the simple 6-stage kanban with a full 3-pipeline system:

**Pipeline 1 — Waitlist & Launch** (gold #C9A96E):
| Stage | Key | Trigger |
|-------|-----|---------|
| 🟡 Waitlist Joined | `waitlist_joined` | Form submitted |
| 📧 Welcome Sent | `welcome_sent` | 2 min after signup |
| 🔥 Hot Lead | `hot_lead` | Engagement trigger |
| 📞 Personal Outreach | `personal_outreach` | Manual move |
| ✅ Trial Activated | `trial_activated` | Login confirmed |
| ❌ No Show | `no_show` | No activation 7 days |

**Pipeline 2 — Trial to Paid** (teal #2ECC9A):
| Stage | Key | Trigger |
|-------|-----|---------|
| 🟡 Trial Day 1-7 | `trial_day_1_7` | Trial starts |
| 🔄 Trial Day 8-14 | `trial_day_8_14` | Auto day 7 |
| 🔥 Trial Day 15-21 | `trial_day_15_21` | Auto day 14 |
| 💬 Demo Booked | `demo_booked` | Calendar booked |
| 💳 Proposal Sent | `proposal_sent` | Post-demo manual |
| 🏆 Won / Paid | `won_paid` | Payment confirmed |
| 😴 Lost / Churned | `lost_churned` | Trial ended no convert |
| ⏸️ Nurture | `nurture` | Not ready signal |

**Pipeline 3 — Active Clients** (purple #8B5CF6):
| Stage | Key | Trigger |
|-------|-----|---------|
| 🟢 Onboarding | `onboarding` | Won auto-trigger |
| ✅ Live & Healthy | `live_healthy` | Onboarding complete |
| ⚠️ At Risk | `at_risk` | 14 days no login |
| 💰 Upsell | `upsell` | Usage threshold |
| 🏆 Advocate | `advocate` | Happy signals |
| ❌ Churned | `churned` | Cancellation |

### 4. Email Automation Engine
- Every stage has an email template in `STAGE_AUTOMATIONS` object (Edge Function)
- When a lead moves to a new stage, the matching email fires automatically via Resend
- Token replacement: `{{first_name}}`, `{{last_name}}`, `{{email}}`, `{{state}}`, `{{tier_target}}`, `{{weekly_fee}}`, `{{annual_value}}`, `{{implementation_fee}}`
- All sent emails stored in `admin_emails` table
- `waitlist_joined` sends internal notification to info@titus-crm.com (not to lead)
- "Skip auto-email" checkbox in Lead Detail Panel to override
- All emails signed as "A4, Founder — Titus CRM"

### 5. Onboarding Tracker
- Pipeline 3 / `onboarding` stage shows 8-step checklist:
  1. Onboarding call scheduled
  2. Data migration complete
  3. SCHADS compliance configured
  4. AI features activated
  5. Team training completed
  6. First live roster created
  7. Day 7 go-live check
  8. Fully live confirmed
- Stored as `onboarding_tasks` JSONB on waitlist table
- Progress counter (X/8 complete)

### 6. UI Improvements
- Background changed to warm cream (#FAF7EE / #F0EDE4) for readability
- Pipeline selector tabs in Kanban view
- Cards show pipeline dot, stage emoji, days-in-stage counter (red after 7d)
- Dashboard shows Pipeline 1/2/3 counts with colored stat cards
- Export includes Pipeline + Stage columns

---

## Architecture

```
www.titus-crm.com
├── / → index.html (marketing site)
├── /agreement-builder → agreement-builder.html
├── /administrator → administrator.html (password: Valencia@@)
└── email handler → Cloudflare Email Worker

Edge Function: agreement-api (octdvaicofjmaetgfect)
├── POST /waitlist → create lead + auto-tag
├── POST /admin/login → auth token
├── GET /admin/leads → list all leads
├── PATCH /admin/leads/:id → update lead + trigger automation email
├── GET /admin/emails → list admin emails
├── POST /admin/email/send → send email via Resend
├── POST /admin/email/inbound → webhook from CF Worker
├── GET /support-items → NDIS support items
├── POST /agreements/public → create agreement
└── GET /agreements/download/:id → download PDF
```

## Database: `waitlist` table (Supabase)

| Column | Type | Notes |
|--------|------|-------|
| id | uuid | PK |
| first_name | text | |
| last_name | text | |
| email | text | |
| phone | text | |
| state | text | |
| provider_types | text[] | |
| business_structures | text[] | |
| interested_features | text[] | |
| status | text | Stage key (e.g. `waitlist_joined`) |
| pipeline | integer | 1, 2, or 3 |
| tags | text[] | e.g. `['WAITLIST FORM']` |
| notes | text | Admin notes |
| onboarding_tasks | jsonb | Array of {label, done} |
| trial_start_date | timestamptz | |
| trial_end_date | timestamptz | |
| created_at | timestamptz | |
| updated_at | timestamptz | |

## Database: `admin_emails` table

| Column | Type | Notes |
|--------|------|-------|
| id | uuid | PK |
| direction | text | `inbound` or `outbound` |
| from_address | text | |
| to_address | text | |
| subject | text | |
| body_html | text | |
| lead_id | uuid | Optional FK to waitlist |
| created_at | timestamptz | |

---

## Credentials & Secrets

| Service | Key | Location |
|---------|-----|----------|
| Admin Portal | Password: `Valencia@@` | Hardcoded in Edge Function |
| Supabase | Project: `octdvaicofjmaetgfect` | ap-southeast-2 (Sydney) |
| RESEND_API_KEY | `re_i2SDT4Au_...` | Supabase Edge Function secrets |
| Cloudflare | Account: `b52d78b0731686babcdbfb81cb71d77f` | |
| Email forward | `tasigale68@gmail.com` | wrangler.toml FORWARD_EMAIL |

## Deploy Commands

```bash
# Edge Function
cd ~/titus-crm/website-deploy
npx supabase functions deploy agreement-api --project-ref octdvaicofjmaetgfect --no-verify-jwt

# Cloudflare Worker (must be logged in: npx wrangler login)
cd ~/titus-crm/website-deploy
npx wrangler deploy
```

---

## Completed Actions

- [x] **RESEND_API_KEY** set in Supabase Edge Function secrets
- [x] **Resend domain verified** for titus-crm.com
- [x] **Cloudflare Email Routing** enabled — MX records added, Google MX removed
- [x] **Email routing rule** created: info@titus-crm.com → Worker `tituscrm` (Active)
- [x] **Tags system** built — auto-tag `WAITLIST FORM`, editable in UI, in exports
- [x] **3-Pipeline CRM** — 19 stages across 3 pipelines, kanban with pipeline tabs
- [x] **Email automation engine** — 20 stage templates, auto-send on stage change via Resend
- [x] **Onboarding tracker** — 8-step checklist for Pipeline 3 onboarding stage
- [x] **UI overhaul** — warm cream background, pipeline badges, days-in-stage, dashboard stats
- [x] **SQL migration** — pipeline, tags, onboarding_tasks, trial dates columns added; existing leads migrated to `waitlist_joined` status with `WAITLIST FORM` tag
- [x] **Edge Function deployed** — agreement-api with all automation logic
- [x] **Cloudflare Worker deployed** — administrator.html with full 3-pipeline UI
- [x] **Verified live** — 2 leads confirmed showing `waitlist_joined`, Pipeline 1, tagged

## Current Status (as of 2026-03-11)

**Everything is LIVE and working:**
- Admin portal: https://www.titus-crm.com/administrator (password: Valencia@@)
- 2 leads in Pipeline 1 (Michael Jones, tasi gale) — both `waitlist_joined`
- Email send/receive active via info@titus-crm.com
- Auto-emails fire when dragging cards between stages
- Inbound emails forwarded to tasigale68@gmail.com + stored in admin portal

## Pending (Nice to Have)

- [ ] Convert marketing SPA to static HTML for SEO
- [ ] Submit to Google Search Console
- [ ] Set up Cloudflare Email Routing catch-all (currently only `info@` routes to Worker)
- [ ] Add calendar link placeholders in email templates (replace `[INSERT CALENDAR LINK]`)
- [ ] Add SMS sending capability (currently email-only automations)
- [ ] Cron job for time-based automations (e.g., auto-move to `no_show` after 7 days)
- [ ] Add `updated_at` auto-trigger on waitlist table for accurate days-in-stage tracking

---

## Email Playbook Reference
The full email playbook with all templates, SMS scripts, and task definitions is at:
`~/titus-crm/website-deploy/docs/email-playbook.html`
(19 stages × 3 pipelines, ~35 email templates, SMS scripts, internal tasks)

## Supabase CLI Access
- **Access token:** stored at `~/.supabase/access-token`
- **Management API:** Can run SQL via `POST https://api.supabase.com/v1/projects/{id}/database/query` with Bearer token
- **Supabase CLI auth:** Working — `npx supabase projects list` shows all projects
- **Wrangler auth:** Requires `npx wrangler login` (browser-based, expires)
