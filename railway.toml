[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
# startCommand is NOT set here — each Railway service sets its own
# in the dashboard Settings → Custom Start Command:
#   Web server:  node src/server.js
#   Sync worker: node worker.js
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
